import defaultBackground from "../assets/wallpapers/default_wallpaper.jpg";
import { DefaultTheme } from "styled-components";

export const theme: DefaultTheme = {
  background: defaultBackground,
};
